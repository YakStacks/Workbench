/**
 * Agent Handoff Protocol (AHP) — public API barrel
 *
 * runtime.ts and types.ts are removed; the AHP packet lifecycle is now handled
 * by the Mailman runtime (src/mailman/index.ts).
 */
export * from './chain-executor';
